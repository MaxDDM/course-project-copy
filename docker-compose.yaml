version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: workout-log-api:latest
    container_name: workout-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
